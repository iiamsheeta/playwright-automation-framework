{
  "name": "qa-automation",
  "version": "1.0.0",
  "main": "index.js",

  "scripts": {
  "start": "ng serve --host 0.0.0.0 --port 4200",
  "test:e2e": "npx playwright test"
},

  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^25.3.0"
  },

  "dependencies": {
    "dotenv": "^17.3.1",
    "wait-on": "^8.0.5"
  }
}